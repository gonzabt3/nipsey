<%= form_with(model: career) do |form| %>
  <% if career.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(career.errors.count, "error") %> prohibited this career from being saved:</h2>

      <ul>
        <% career.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :university_id, style: "display: block" %>
    <%= form.text_field :university_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
